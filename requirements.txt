Django>=6.0,<6.1
gunicorn>=22.0
whitenoise>=6.7
dj-database-url>=2.2
psycopg2-binary>=2.9
python-dotenv>=1.0
anthropic>=0.39
pydantic>=2.8
cryptography>=42.0
# Feature 3 — RAG Roadmap dependencies
# Core pipeline runs with NONE of these (hashing embedder + template LLM fallback).
# Install these for production-quality retrieval + synthesis.

# Embeddings (multilingual FR + AR) — primary embedder
sentence-transformers>=3.0.0
torch>=2.2.0

# Vector store (production) — optional; in-memory store used otherwise
qdrant-client>=1.9.0

# KB ingestion from Excel drafts
openpyxl>=3.1.0

# Grounded assistant (Sonnet handoff) — optional; uses your Anthropic Pro key
anthropic>=0.39.0

# Local roadmap LLM is reached over HTTP (Ollama) — no Python package required.
# Install Ollama separately:  curl -fsSL https://ollama.com/install.sh | sh
#                             ollama pull qwen2.5:7b-instruct
